import "io"

fn main() {
	var empty = [] int
	var array = [5] int

	//Regular index
	printf("array[%d] is %d\n", 0, array[0])

	//Iteration
	for int i = 0; i < 5; i = i + 1 {
		array[i] = i
		printf("array[%d] is %d\n", i, array[i])
	}

	
}
